/* app/globals.css - MA TRANSFORM LAB */
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --black: #000000;
  --dark-purple: #1a1a2e;
  --blue: #1E3A8A;
  --light-navy: #3B82F6;
  --gold: #FFC107;
  --yellow: #FFD700;
  --orange: #FF5722;
  --orange-light: #FF9800;
  --purple: #6B46C1;
  --purple-light: #8E44AD;
  --green: #25D366;
  --white: #FFFFFF;
  --light-gray: #bfc4d6;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  line-height: 1.6;
  color: var(--white);
  background: linear-gradient(180deg, var(--black) 0%, var(--dark-purple) 40%, var(--blue) 70%, var(--light-navy) 100%);
  overflow-x: hidden;
  min-height: 100vh;
  font-family: system-ui, -apple-system, sans-serif;
}

/* Glass Effect */
.glass {
  background: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.18);
}

.glass-card {
  background: rgba(0, 0, 0, 0.8);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255, 255, 255, 0.18);
  border-radius: 12px;
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
}

/* Gradient Text */
.gradient-text-gold {
  background: linear-gradient(45deg, #FFC107, #FFD700, #FF5722);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.gradient-text-yellow {
  background: linear-gradient(45deg, #FFD700, #FF5722);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.gradient-text-orange {
  background: linear-gradient(45deg, #FF5722, #FF9800);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* Container utility */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
}
